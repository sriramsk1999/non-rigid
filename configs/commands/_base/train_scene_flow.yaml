# @package _global_
defaults:
    - /train@_here_
    - override /model: df_base
    - override /dataset: proc_cloth
    - _self_

model:
    type: flow

dataset:
    type: flow
    scene: True
    world_frame: True
